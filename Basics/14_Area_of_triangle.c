#include<stdio.h> 

int main(){
    int base,height;
    printf("Enter the base of a triangle ");
    scanf("%d",&base);
    printf("Enter the height of a triangle");
    scanf("%d",&height);
    printf("Area of a triangle is %f",0.5*base*height);
    return 0;
}